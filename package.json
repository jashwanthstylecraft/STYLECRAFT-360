{
  "name": "stylecraft-360",
  "version": "0.1.0",
  "private": true,
  "description": "StyleCraft 360 — internal executive dashboard. The whole business, at a glance.",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev:all": "concurrently -n CLIENT,SERVER -c blue,magenta \"npm run dev\" \"npm run server\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
